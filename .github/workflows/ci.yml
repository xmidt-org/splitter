# SPDX-FileCopyrightText: 2026 Comcast Cable Communications Management, LLC
# SPDX-License-Identifier: Apache-2.0
---
name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md
      - CONTRIBUTING.md
      - MAINTAINERS.md
      - LICENSE
      - NOTICE
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    uses: xmidt-org/shared-go/.github/workflows/ci.yml@3d7b27cc01d8e8c7a1d24f96c4c57688bc8f8fac # not released
    with:
      release-project-name: splitter
      release-type:         program
      release-main-package: ./cmd/splitter
      release-arch-amd64:   true
      release-arch-arm64:   true
      release-docker:       false
      yaml-lint-skip:       false
    secrets: inherit
